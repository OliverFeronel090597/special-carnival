# Step 1: Initialize repo
git init

# Step 2: Create and switch to main branch
git branch -M main

# Step 3: Add remote
git remote add origin https://github.com/OliverFeronel090597/Index-Test-Time-Tool.git

# Step 4: Add & commit
git add .
git commit -m "Initial commit"

# Step 5: Push main branch
git push -u origin main


if error

git init
git branch -M main
git remote add origin https://github.com/OliverFeronel090597/Index-Test-Time-Tool.git

git pull origin main --allow-unrelated-histories

git add .
git commit -m "Initial commit"
git push -u origin main



quick check

git branch
git remote -v
git status


Senior dev tip

Lock this in early:

git config --global init.defaultBranch main

git --no-pager log --oneline --decorate --graph --parents # check records